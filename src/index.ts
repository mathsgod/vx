import {
    Vx,
    createDescriptons,
    createForm,
    createTable,
    $axios,
    getCurrentInstance,
    getObject
} from "./lib";

import routes from "./routes";

export {
    Vx,
    createDescriptons,
    createForm,
    createTable,
    $axios,
    getCurrentInstance,
    getObject,
    routes
};