import './style/index.css';

import {
    Vx,
    createDescriptions,
    createForm,
    createTable,
    $axios,
    getCurrentInstance,
    getObject,
    getMe,
    getData,
} from "./lib";

import routes from "./routes";

export {
    Vx,
    createDescriptions,
    createForm,
    createTable,
    $axios,
    getCurrentInstance,
    getObject,
    getMe,
    getData,
    routes
};