import {
    Vx,
    createDescriptions,
    createForm,
    createTable,
    $axios,
    getCurrentInstance,
    getObject,
    getMe
} from "./lib";

import routes from "./routes";

export {
    Vx,
    createDescriptions,
    createForm,
    createTable,
    $axios,
    getCurrentInstance,
    getObject,
    getMe,
    routes
};